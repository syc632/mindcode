# 参与贡献

欢迎提交 Issue 和 Pull Request。请先搜索已有问题，描述实际行为、预期行为、复现步骤以及操作系统和 Node.js 版本。

## 本地开发

使用 Node.js 22.12+（建议 24 LTS），Fork 仓库后运行：

```bash
npm ci
npm run dev
```

## 提交变更

1. 从 `main` 创建功能分支，每次聚焦一个问题。
2. 保持现有代码风格，避免无关重构。
3. 数据写入前使用 `src/shared/schema.js` 中的规范化函数。
4. 修改业务逻辑时补充有意义的测试；更新对应使用说明。
5. 运行以下检查，在 PR 中说明改动与验证结果：

```bash
npm test
npm run lint
npm run build
```

请勿提交 API Key、个人笔记、导出图谱、缓存、依赖目录或打包产物。分享错误日志或截图前，请移除其中的凭据和个人资料。

## 许可证

提交的贡献使用本项目的 [MIT 许可证](LICENSE)。引入第三方代码或素材时，请保留其原有许可证和署名。
